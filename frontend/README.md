# Frontend

React + Vite + TypeScript frontend for BudgetSpace AI.

## Run

```bash
npm install
npm run dev
```

The frontend expects the backend at:

```text
http://localhost:8080
```

Override it with:

```bash
cp .env.example .env
```

Then edit `VITE_API_BASE_URL`.
